/* assets/tab/config.wxss */
.tab-icon {
  width: 48rpx;
  height: 48rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

.icon-text {
  font-size: 20rpx;
  color: var(--muted);
}
