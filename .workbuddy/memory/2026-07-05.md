# 2026-07-05 Veloform 页面完善

## 完成内容
- 新增 `loading.tsx` (首页/配置库的骨架屏)
- 新增 `not-found.tsx` (自定义 404 页面)
- 首页集成 Pricing + Cta sections
- Navbar 集成 SupportModal + OnboardingGuide
- 修复 vercel.json (Angular→Next.js)
- 清理 next.config.mjs (Firebase→Supabase)
- 移除 next-pwa (依赖冲突)

## 构建问题
- 沙盒环境无法加载原生 .node 模块 (SWC, esbuild)
- 解决方案: `npm install --ignore-scripts` + 手动安装 `@next/swc-wasm-nodejs`
- 修改 `node_modules/next/dist/build/swc/index.js` 强制启用 WASM 回退
- 构建成功: Router 全部通过，0 错误
